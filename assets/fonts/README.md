# Fonts

Place `rat-typewriter.ttf` or any compatible `.ttf` / `.otf` dialogue font here when the team is ready.

The reusable dialogue theme is at `res://themes/dialogue_theme.tres`. After importing the font in Godot, assign it to the Theme's default font or to the Label/RichTextLabel font overrides used by the dialogue UI.
